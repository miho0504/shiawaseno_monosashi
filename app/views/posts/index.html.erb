<%= link_to '仮日記作成ボタン', new_post_path %>

<%= month_calendar events: @posts do |date, posts| %>
  <%= date.day %>

  <% posts.each do |p| %>
    <div>
      <%= link_to p.today_mood, p %>
    </div>
  <% end %>
<% end %>