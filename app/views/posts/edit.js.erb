var modal = document.getElementById('modal');

var mask = document.getElementById('mask');

modal.innerHTML = '<%= escape_javascript(render 'edit', post: @post ) %>';

<!--editはnewで戻る処理がされているからいらない-->